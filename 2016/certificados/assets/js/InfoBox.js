import React from 'react';

export default class InfoBox extends React.Component {
  render() {
    return (
      <div>
        <p>Use o formulário ao lado para gerar seu certificado de participação. Todos os campos tem preenchimento obrigatório e devem corresponder com os dados usados durante a inscrição do evento.</p>
      </div>
    );
  }
}